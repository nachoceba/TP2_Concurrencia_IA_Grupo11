# Declaración de Uso de IA (DUIA) - Parte 3: Optimización de Consultas e Índices

| _Herramienta_ | OpenCode (modelo Muse Spark vía Meta MSL) |
| _Spec o prompt utilizado_ | _Tarea 1 (Índices y reescrituras):<br>"Basado en las capturas de pantalla agregadas al proyecto, necesito que propongas reescrituras y/o índices que mejoren el tiempo de ejecución de las consultas."<br><br>Tarea 2 (Tabla de resultados):<br>"En base a las 3 consultas que aparecen en las capturas de pantalla, necesito que rellenes el archivo '2.2 Tabla de resultados' con las siguientes columnas y su respectiva información: Consulta, Plan antes (nodo, cost, tiempo real), Cambio aplicado, Plan después (nodo, cost, tiempo real), Mejora."<br><br>Tarea 3 (Lectura crítica):<br>"Basándote en los índices del archivo indices.sql (seleccioná 3) necesito que crees un archivo Excel llamado '4. Lectura crítica' con las columnas y rellenalo con la info correspondiente: Afirmación de la IA, ¿Correcta? (poner si o no), Corrección / evidencia del plan real."_ |
| _Qué generó_ | _Tarea 1:<br>El archivo `indices.sql` con el diagnóstico de las 3 capturas (`EXPLAIN ANALYZE SELECT _ FROM usuario / producto / pedido`, con Seq Scan de ~1.075 ms / ~2.720 ms / ~10.707 ms), 12 índices propuestos con `CREATE INDEX IF NOT EXISTS`(parciales`WHERE eliminado = FALSE`, compuestos `idx_pedido_usuario_fecha`e`idx_pedido_estado_fecha`, covering `idx_detalle_pedido_covering`, GIN trigram con `pg_trgm`) y 7 consultas reescritas (Q1-Q7) con su verificación esperada, todo bajo el protocolo de `protocolo_seguridad.md`(clon, transacción, respaldo).<br><br>Tarea 2:<br>El relleno de`TP 3/2.2) Tabla de resultados.xlsx`(Hoja 1) con encabezado + 3 filas, una por cada consulta de las capturas, con el plan antes transcripto (nodo Seq Scan, cost, tiempos reales) y el cambio aplicado tomado de`indices.sql`.<br><br>|
| *Qué se aceptó* | Se aceptó el diagnóstico central de la IA: un `SELECT *`sin`WHERE`obliga a Seq Scan y ningún índice B-tree lo evita, por lo que la mejora real viene de reescribir a consultas selectivas (columnas +`WHERE`+`LIMIT`) e indexar sus predicados. Se aceptaron los índices compuestos y parciales propuestos y el criterio de consignar el "Plan después" como esperado (a medir en clon) en lugar de inventar cifras. |
| *Qué se modificó o descartó* | Se descartó la idea de indexar los `SELECT *`literales de las capturas: se los dejó como "caso testigo" en Seq Scan. Se agregaron notas explícitas de que los`rows=`de las capturas están truncados (inferidos de`data.sql`: 20k/50k/200k) y de que los planes "después" deben confirmarse con `EXPLAIN (ANALYZE, BUFFERS, TIMING)` en el clon. Los nombres finales de los Excel (`2.2) Tabla de resultados.xlsx`, `3.4) Lectura crítica.xlsx`) se ajustaron manualmente a la numeración de la consigna de la cátedra. |
| *Verificación realizada* | Se verificó por lectura directa que `2.2) Tabla de resultados.xlsx`contiene el rango`A1:E5`(encabezado + 3 filas + nota) y que`3.4) Lectura crítica.xlsx`contiene`A1:C5`con la validación SÍ/NO en la columna B. Se contrastó que cada columna e índice citado existe en`schema.sql`e`indices.sql`(nombres de tablas, columnas y predicados`eliminado = FALSE`). Queda pendiente la verificación en motor: aplicar `indices.sql`sobre el clon según`protocolo_seguridad.md` y re-mediar cada reescritura para completar los tiempos reales del "Plan después". |
